# コエリアル(音声版BeReal)開発ロードマップ

夜間開発エージェント用バックログ。**上から順に、1回1項目だけ**実装し、完了したらチェックを付けてこのファイルも一緒にコミットすること。

## バックログ(優先順)

- [x] Web Push通知(アプリ化の核・オーナー承認済み): VAPIDキー生成(環境変数)+購読をSupabaseに保存(subscriptionsテーブル、migrations/に SQL出力)+Vercel Cron+Functionで毎日ランダム時刻(11:00-21:00 JST)に「いまの声を残そう」を配信。sw.jsにpushイベント処理を追加
- [x] ホーム画面追加オンボーディング: 初回ログイン後に「1. 共有ボタン → 2. ホーム画面に追加 → 3. 通知をオン」をOS別(iOS/Android)に図解する案内画面。スキップ可・プロフィールから再表示可
- [x] パスワードリセット: ログイン画面に「パスワードを忘れた場合」リンク。supabaseのresetPasswordForEmailで再設定メール送信+再設定画面(検収中にオーナーが詰まった実績ありの優先課題)
- [x] 毎日のお題時刻の表示: その日のランダム時刻(11:00-21:00)を全員共通で表示し、「時刻から00分以内の投稿」にバッジを付ける(時刻はseed=日付のハッシュで全クライアント一致)
- [x] フレンド機能(第1段): 自分の「フレンドコード」(短い英数字)を表示し、相手のコードを入力すると相互フレンドになる。friendships テーブル+RLS+migrations/にSQL出力
- [x] フレンドのフィード: フレンドの「きょうの声」がホームに並ぶ(本人の投稿の上)。RLSで「フレンドのみ参照可」を厳密に
- [x] リアクション: フレンドの投稿に「聴いたよ」ワンタップ(1投稿1回)
- [x] 録音の波形ビジュアライザ: 録音中にWeb Audio APIで音量バーを表示
- [x] OGP整備: トップページのOGPタグ+OGP画像(SVG)。アプリ自体は非公開データなので紹介ページとしてのOGP
- [x] PWAのオフライン改善: シェル(HTML/CSS)のキャッシュ戦略をsw.jsに実装
- [x] アクセシビリティ監査: スクリーンリーダーでの操作確認、aria属性の改善
- [x] 投稿の月別アーカイブ表示: プロフィールにカレンダー形式で投稿日を表示
- [x] フレンド一覧・削除: プロフィール画面にフレンドのリストを表示し「解除」ボタンでフレンドを解除できる(RPC: remove_friend。オーナー作業: migrations/005-remove-friend.sql を実行)
- [x] 音声の再生速度変更: フィードのプレイヤーに「x0.75 / x1 / x1.5」切り替えボタンを追加
- [x] フレンドのひとことをフィードに表示: フレンドの投稿カードにbio(ひとこと)を小さく表示する
- [x] 連続記録ストリークバナー: 3日以上連続で投稿したときフィード上部に「n日連続！」バナーを表示
- [x] 自分の過去の声をカレンダーから聴き直し: プロフィールのカレンダーの投稿済み日をタップすると音声が再生される
- [x] フレンド招待テキスト生成: フレンドコードを含む定型招待文をクリップボードにコピーするボタン
- [x] 投稿前プレビュー再生: 録音終了後にすぐアップロードせず、録音内容を「再生して確認」→「この声で投稿する」か「撮り直す」を選べるようにした
- [x] 録音中のキャプション文字数カウンター: 録音中のキャプション入力欄に残り文字数(50文字中)をリアルタイム表示する
- [x] フレンド一覧に投稿状況表示: フレンド一覧でそのフレンドがきょう投稿済みかどうかを小さいバッジで示す(DB追加なし・既存postsテーブル参照のみ)
- [x] フィード手動更新ボタン: フィード上部に「更新」ボタンを追加し、プルダウン操作の代わりに手動でフィードを再読み込みできる
- [x] 自分の投稿に対するリアクション通知表示: プロフィール画面に「最近のリアクション」を表示し、誰がいつ「聴いたよ」をしたかと対象投稿の日付を一覧できる
- [x] 自分の投稿を削除: フィードの自分の投稿カードに「削除」ボタンを追加。確認ダイアログ後にDB(posts)とStorage(voice-posts)から削除(postsテーブルにDELETE RLSポリシーが必要: `CREATE POLICY "users can delete own posts" ON posts FOR DELETE USING (auth.uid() = user_id);` をオーナーが実行)
- [x] 今日未投稿のフレンド一覧: フィードで今日まだ声を残していないフレンドを「まだ声がないよ」として一覧表示(DB追加なし・friend_feedビュー参照のみ)
- [x] プロフィールの投稿統計: 合計投稿数・最長連続記録日数をプロフィール画面に表示(DB追加なし)
- [x] 投稿時の気分タグ: 録音前に気分タグ(朝・夜・ひとりごと・仕事中 等)を1つ選択。postsテーブルにtag列を追加(migrations/006-post-tag.sql)
- [x] フレンドの過去投稿アーカイブ: フレンド一覧からフレンドの投稿カレンダーを閲覧できるモーダル(RLS: friend_feed経由のみ・自分のフレンドのみ)
- [x] プッシュ通知クリックでフィードを開く: sw.jsのnotificationclickイベントでアプリのフィード画面にフォーカス/遷移するよう改善
- [x] プロフィールタブの未読リアクションバッジ: プロフィールタブに新着リアクション件数を赤バッジで表示。プロフィールを開くとリセット(DB追加なし・localStorageで既読管理)
- [x] ダークモード/ライトモード切り替え: システム設定に連動しつつ、プロフィール画面から手動切替も可能(localStorageで保存・DB追加なし)
- [x] テキストコメント機能: フレンドの投稿に短いテキストコメント(30文字以内)を残せる。commentsテーブル追加(migrations/007-comments.sql)
- [x] フレンドのミュート: フレンドのままフィードに表示しないミュート機能。muted_friends列をprofilesテーブルにjsonb型で追加(migrations/008-muted-friends.sql)
- [x] 投稿のブックマーク: フレンドの投稿をお気に入りとして保存・一覧できる。bookmarksテーブル追加(migrations/009-bookmarks.sql)
- [x] フィードのタグ別フィルター: フィード上部に「全て/朝/夜/ひとりごと/仕事中」の絞り込みボタンを追加。タグ付き投稿が1件以上ある場合のみ表示。DB変更なし
- [x] 波形バークリックでシーク: フィードとブックマーク一覧のプレイヤーで、波形バーをクリック/タップすると再生位置をジャンプできる。再生中のトラックはシーク、停止中のトラックはクリック位置から再生開始。DB変更なし
- [x] 自分の投稿へのコメント通知: プロフィール画面に「最近のコメント」一覧を追加し、フレンドが自分の投稿に残したコメントを表示。プロフィールタブのバッジをリアクション+コメントの合計件数に拡張(DB変更なし・commentsテーブル参照のみ)
- [x] 自分のコメントの削除: フレンドの投稿に書いたコメントに「x」ボタンを表示し、自分のコメントのみ削除できる。commentsテーブルにDELETE RLSポリシーを追加(migrations/010-comment-delete-rls.sql)
- [x] フィードの新着自動検知: フィードを開いているときにフレンドが投稿すると「フレンドの新しい声がn件とどいたよ」バナーを表示。1分ごとにfriend_feedビューをポーリングして差分チェック(DB変更なし)
- [x] 自分の投稿のキャプション編集: フィードの自分の投稿カードに「編集」ボタンを追加。キャプションをインライン編集・保存できる(50文字以内)。postsテーブルにUPDATE RLSポリシーが必要(migrations/011-post-caption-update-rls.sql をオーナーが実行)
- [x] 「聴いたよ」件数をフィードカードに表示: 各フレンド投稿の「聴いたよ」ボタンに合計リアクション数を表示(例: 「聴いたよ 3」)。ボタン押下時に楽観的更新。DB変更なし・reactionsテーブル参照のみ
- [x] フィードの「全部聴く」プレイリストモード: フレンドの声セクションに「全部聴く」ボタンを追加。タップするとフレンドの投稿を上から順番に自動再生。再生中は「停止」に切り替わり途中停止も可能。手動で別の投稿を再生するとキューはリセット。DB変更なし
- [x] フィードカードへのコメント数表示: 各フレンド投稿のリアクションボタン横に「コメント N件」を表示。投稿後・削除後に楽観的更新。commentsテーブルをバッチフェッチ。DB変更なし
- [x] 最後まで聴いたら自動「聴いたよ」: フレンドの投稿音声を最後まで再生すると、自動で「聴いたよ」リアクションを付ける。未リアクション時のみ動作。DB変更なし
- [x] 再生済み投稿のグレーアウト: フィードで一度再生ボタンを押した投稿カードを透明度72%に下げて視覚的に区別する。「聴いたよ済み」のカードも同様にグレーアウト。localStorageでユーザー・日付単位に管理。DB変更なし
- [x] 再生速度の記憶: 再生速度ボタン(x0.75/x1/x1.5)の設定をlocalStorageに保存し、次回フィード・ブックマーク読み込み時も前回選択した速度で各カードを初期化する。DB変更なし
- [x] 昨日の声セクション: フィードの一番下に「昨日の声」として前日のフレンド投稿を折りたたみ表示(クリックで展開)。DB変更なし・friend_feedビューに日付フィルターを変えてクエリを追加するだけ
- [x] プロフィール表示名の変更: プロフィール画面でusernameをインライン編集できるようにする(30文字以内)。profilesテーブルにUPDATE RLSポリシーが必要(migrations/012-profile-update-rls.sql)
- [x] フレンドのミニプロフィールカード: フィードカードのフレンド名をタップすると、bio・合計投稿数を表示するミニモーダルが開く。DB変更なし
- [x] 通知ON/OFF設定: プロフィール画面に「毎日の通知を受け取る」トグルを追加。subscriptionsテーブルにis_active boolean列を追加し、Vercel Functionで購読がアクティブなユーザーのみに通知(migrations/013-subscription-active.sql)
- [x] 未聴のみフィルター: フィード上部に「未聴のみ」トグルボタンを追加。localStorage の再生済みセットを参照して未再生カードのみ表示。タグフィルターと組み合わせ可。DB変更なし
- [x] ミニプロフィールにフレンドの現在ストリーク表示: フレンドのミニプロフィールカードに「N日連続中」を追加。friend_feedからpost_dateを全件取得してcalcCurrentStreak関数で計算。DB変更なし
- [x] プロフィールのひとこと(bio)をインライン編集に変更: 常時表示のtextarea+保存ボタン形式から、表示名編集と同じインライン編集スタイルに統一(編集ボタン→入力→保存/キャンセル)。DB変更なし
- [x] フィードカードに音声の長さ(秒数)を表示: HTMLのaudio要素のloadedmetadataイベントで各投稿の録音時間を取得し、カード上に「XX秒」と表示。DB変更なし
- [x] フィードのキャプション検索バー: フィード上部に検索入力欄を追加し、キャプションでフレンドの投稿を絞り込む。クライアントサイドフィルター。DB変更なし
- [x] 録音最大時間の選択: 録音画面で15秒/30秒/60秒を選択可能にする。localStorageで保存。DB変更なし

## ルール

- 絵文字は使用しない(UI・文言・コミットメッセージとも。✓ ✕ → などの記号は可)
- 1回1項目のみ。差分は小さく(目安250行以内)
- 使用してよい外部ライブラリは supabase-js(esm.sh経由)のみ。他は追加禁止
- データベースのスキーマ変更が必要な場合は、SQLを migrations/NNN-名前.sql として出力し、コードは「テーブルが無い場合のエラー表示」に対応させる(SQLの実行はオーナーが行う)
- RLS(行レベルセキュリティ)を絶対に緩めない。「他人のデータは見えない」が最重要要件
- Supabaseの secret キーをコードに書かない(publishableキーのみ使用可)
- UI文言はすべて日本語。トーンは明るくフレンドリー
- 変更後はHTML構文とJS構文を確認してからプッシュ
- 確信が持てなければ見送ってよい(本番に直接反映されるため)
